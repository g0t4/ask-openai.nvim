## code preferences
- lua: snake_case by default, classes/constants in PascalCase
- python: snake_case by default, PascalCase for classes
- when rewriting code, leave unrelated code and unrelated comments as they are

