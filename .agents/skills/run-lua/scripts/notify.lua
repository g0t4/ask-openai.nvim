---@param msg string
local function notify_user(msg)
  vim.notify(msg, vim.log.levels.INFO)
end

-- Example usage (uncomment to run when this script is executed directly)
-- notify_user("Hello from run_lua skill script!")

