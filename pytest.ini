[pytest]
# Only run tests located in the top-level ``tests`` directory.
testpaths = tests
# Prevent pytest from recursing into the ``lua`` directory, which contains many
# Python test files that depend on optional modules not present in the CI
# environment.
norecursedirs = lua
